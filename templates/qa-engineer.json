{
  "name": "qa-engineer",
  "version": "1.0.0",
  "description": "A QA testing agent — writes tests, runs test suites, and reports quality issues",
  "backend": { "type": "claude-code" },
  "domainContext": {
    "skills": ["test-writer"],
    "prompts": ["qa-assistant"],
    "mcpServers": [
      {
        "name": "filesystem",
        "command": "npx",
        "args": ["-y", "@modelcontextprotocol/server-filesystem", "."]
      }
    ]
  },
  "metadata": {
    "category": "web-dev",
    "difficulty": "beginner"
  }
}
